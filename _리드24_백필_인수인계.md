# 리드24 백필 인수인계 (2026-08-08 시작)

## 왜 받고 있나

통계리포트 §9.5 가 재 봤다 — **밤을 2배로 모으면 총시잉 예보 상관이 +0.054** 오른다.
그런데 병목이 라벨이 아니라 **GEFS** 였다.

| | 라벨(DIMM·MASS 3성분) | GEFS | 학습에 쓸 수 있는 밤 |
|---|---|---|---|
| 2015–2016 | 540 | 0 | 0 |
| 2017–2023 | 2,182 | 2,554 | **2,179** |
| 2024–2026 | 883 | 1 | 1 |

라벨은 창고에 2015~2026 이 다 있다(3,604밤). GEFS 수집이 `--end 2023-12-31` 에서 멈춰 있었을 뿐이다.
2024~2026 을 채우면 **2,180 → 3,063밤(1.4배)**.

## 지금 돌고 있는 것

```bash
python -X utf8 backfill_lead.py --member <0~4> --start 2024-01-01 --end 2026-08-07
```

- 멤버마다 **프로세스 하나**, 5개 병렬. 로그는 `logs_lead24_m<M>.log`
- **스레드로 돌리지 말 것** — eccodes 가 깨진다 (2026-08-08 실측: `grib_handle_new_from_file_multi` 오류)
- 00Z **리드 24 만** 받는다. 2024년부터 GEFS 가 3시간 간격이라 하루에 리드가 30개 넘는데,
  한 단위가 약 160초라 전체 스펙은 노트북에서 몇 달이다. 정본 분석이 쓰는 것은 리드24 하나뿐이다(§2.2).
- 출력은 기존과 같은 규약 `data/v2/{YYYY-MM}_m{M}.jsonl.gz` — 나중에 다른 리드를 덧붙여도 그대로 합쳐진다
- **git 은 안 건드린다.** `worker_v2.py` 는 주기적으로 commit·push 하지만 이 러너는 안 한다

## 껐다 켠 뒤에 할 일 — 순서대로

### 1) 끊긴 파일부터 고친다 (전원이 갑자기 나갔으면 필수)

```bash
python -X utf8 verify_repair_v2.py          # 검사만
python -X utf8 verify_repair_v2.py --fix    # 끊긴 파일을 읽히는 데까지만 남기고 다시 씀
```

왜 필요한가: 수집기는 gzip 을 **덧붙이기**로 쓴다. 전원이 나가면 마지막 조각이 잘리는데,
그대로 두면 ① 그 파일을 읽을 때 예외가 나고 ② 재개 로직이 파일 전체를 버려 이미 받은 밤을
다시 받고 ③ 그 뒤에 또 덧붙이면 깨진 조각이 가운데에 영영 남는다.
원본은 `.broken.<시각>` 으로 남으니 잃는 것은 없다.

### 2) 이어서 받는다

같은 명령을 그대로 다시 돌리면 된다. **재개형**이라 이미 받은 (cycle, lead) 는 건너뛴다.

```bash
for M in 0 1 2 3 4; do
  nohup python -X utf8 backfill_lead.py --member $M --start 2024-01-01 --end 2026-08-07 > logs_lead24_m$M.log 2>&1 &
done
```

### 3) 다 받으면 창고에 넣고, **캐시를 다시 만들고**, 재측정

```bash
python -X utf8 "../_warehouse/build.py" --pull --prune --mirror
cd "../기상수치모델/사전리서치/seeing-lab"
python -X utf8 build_xy_cache.py --install   # ★ 이걸 빼면 새 밤이 분석에 안 닿는다
python -X utf8 split_fa_gl_budget.py         # 밤이 늘었을 때 상관이 실제로 오르는지
python -X utf8 build_statpack_report.py
```

⚠ **캐시 재생성을 빼면 아무것도 안 바뀐다.** 분석 20여 개가 `out2/statpack/xy_cache.pkl` 을
읽는데, `integrated_v2xy.py` 의 로더는 ① 캐시가 있으면 **무조건 재사용**하고
② 없으면 창고가 아니라 `gefs-collector/data/v2/*.jsonl.gz` 를 직독하는데
그 파일들은 `build.py --prune` 이 지운 상태다(지금 420개 전부 없다).
즉 캐시를 지우고 다시 만들면 **빈 캐시**가 나온다. ERA5 에서 겪은 것과 같은 사고다.

`build_xy_cache.py`(seeing-lab)가 그 자리를 메운다 — 창고 `gefs_v2`·`gefs_v2x`·`gefs_v2y`
에서 읽어 같은 모양의 캐시를 만들고, **옛 캐시와 값 단위로 대조**한 뒤에만 갈아 끼운다.
2026-08-08 검증: 겹치는 12,774행 × 160열에서 **어긋난 칸 0**, 새로 받은 10개만 늘었다.
`integrated_v2xy.py` 는 손대지 않았다 — 그 파일이 만든 옛 캐시가 대조 기준이기 때문이다.

## ★ 노트북으로는 못 받는다 — 실측 결론 (2026-08-08)

| | 처리율 | 4,750단위(리드24 전체)에 걸리는 시간 |
|---|---|---|
| 노트북 1프로세스 | 시간당 23개 | 8.6일 |
| 노트북 5프로세스 | **시간당 14개** | 14일 |
| GitHub Actions 15레인 | 시간당 약 800개 | **6시간** |

노트북 병렬은 **오히려 느려진다.** 30분에 7개밖에 못 받았고, 게다가 경합으로 **가짜 실패**가
났다(m1 은 2024-01-01 을 성공했는데 m0 은 같은 날짜를 실패). cfgrib 디코드가 CPU·디스크를
잡아서 5프로세스가 서로 막는 것이다.

⚠ 가짜 실패를 그대로 두면 안 된다. 3회 실패한 키는 `fails_m*.json` 에 올라가 **영구 스킵**되는데,
그 파일이 커밋되면 Actions 도 그 밤을 건너뛴다. 2026-08-08 에 로컬에서 난 실패는 전부
경합 탓이라 `fails_m*_L24.json` 을 지웠다.

**정공법은 GitHub Actions 다.** 워크플로를 만들어 뒀다 —
`.github/workflows/backfill_lead24.yml` (수동 실행 전용, 멤버 5 × 구간 3 = 15레인).
푸시한 뒤 Actions 탭에서 「Run workflow」 한 번이면 된다.

## 이 일의 값어치 (미리 적어 둠)

- 밤 2,180 → 3,063 = **1.4배**. §9.5 의 로그곡선 외삽으로는 총시잉 **약 +0.03**
  (2배일 때 +0.054 이므로 1.4배는 그 절반쯤)
- 같이 딸려 오는 것: 2024~2026 은 GEFS 신버전 시기라 §6 의 「버전 섞임」 문제에서
  **신버전 표본이 늘어난다** — 구버전 비중이 줄어 판올림 손실이 준다
